// These are not needed if consuming the Artist plugin from Maven Central instead of the local copy from this repo
include ':artist'
include ':artist-api'
include ':artist-traits'
include ':artist-traits-rx'

include ':sample:providers'
